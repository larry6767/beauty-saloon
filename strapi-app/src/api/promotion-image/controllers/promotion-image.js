'use strict'

/**
 * promotion-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories

module.exports = createCoreController('api::promotion-image.promotion-image')
